{
  "name": "bucuresti-vice",
  "private": true,
  "version": "0.1.0",
  "description": "BUCUREȘTI VICE — mici, mașini & gloanțe. Open-world românesc retro (GTA Vice City × NFS Underground × CS), browser-first, local-first.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "~5.6.2",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  }
}
